name: Deploy PHP App to Ubuntu Server

on:
  push:
    branches:
      - main  # Deploy only when pushing to main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up SSH
      uses: webfactory/ssh-agent@v0.9.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
    - name: Deploy via SSH with fallback
      run: |
        ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SERVER_IP }} << 'EOF'
          if [ ! -d "/var/www/html/myapp/.git" ]; then
            echo "Cloning repo as it doesn't exist"
            rm -rf /var/www/html/myapp
            git clone git@github.com:your-username/your-repo.git /var/www/html/myapp
          else
            echo "Repo exists, pulling latest changes"
            cd /var/www/html/myapp
            git pull origin main
          fi
        EOF
